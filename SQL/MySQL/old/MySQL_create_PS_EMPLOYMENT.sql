DROP TABLE IF EXISTS PS_EMPLOYMENT;
CREATE TABLE PS_EMPLOYMENT (
    EMPLID             VARCHAR(11) NOT NULL,
    EMPL_RCD           DECIMAL(38,0) NOT NULL,
    PER_ORG            VARCHAR(3) NOT NULL,
    BENEFIT_RCD_NBR    DECIMAL(38,0) NOT NULL,
    HOME_HOST_CLASS    VARCHAR(1) NOT NULL,
    LAST_DATE_WORKED   DATE,
    HIRE_DT            DATE,
    REHIRE_DT          DATE,
    TERMINATION_DT     DATE,
    SENIORITY_PAY_DT   DATE,
    CMPNY_SENIORITY_DT DATE,
    SERVICE_DT         DATE,
    PROF_EXPERIENCE_DT DATE,
    LAST_VERIFICATN_DT DATE,
    EXPECTED_RETURN_DT DATE,
    LAST_INCREASE_DT   DATE,
    OWN_5PERCENT_CO    VARCHAR(1) NOT NULL,
    PROBATION_DT       DATE,
    NEE_PROVIDER_ID    VARCHAR(10) NOT NULL,
    POSITION_PHONE     VARCHAR(24) NOT NULL,
    BUSINESS_TITLE     VARCHAR(30) NOT NULL,
    REPORTS_TO         VARCHAR(8) NOT NULL,
    SUPERVISOR_ID      VARCHAR(11) NOT NULL
);
